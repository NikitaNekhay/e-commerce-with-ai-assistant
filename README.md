# AI-ассистент для улучшения объявлений маркетплейса

Веб-приложение — личный кабинет продавца с интегрированным AI-ассистентом, который помогает улучшать описания объявлений и оценивать рыночную стоимость товаров.

## Технологический стек

### Frontend
- **React 18** + TypeScript
- **React Router v7** — маршрутизация
- **Redux Toolkit** — управление состоянием
- **Ant Design v6** — UI-компоненты
- **Tailwind CSS v4** — стилизация
- **Axios** — HTTP-запросы
- **date-fns** — форматирование дат
- **Vite** — сборка

### Backend
- **Express.js** + TypeScript
- **OpenRouter API** — интеграция с LLM (Google Gemini 2.0 Flash по умолчанию)

### Инфраструктура
- **Docker** + **docker-compose** — контейнеризация
- **Nginx** — раздача статики и проксирование API
- **Vitest** + **Testing Library** — юнит-тесты

---

## Запуск проекта

###  Docker Compose

```bash
# Убедитесь, что server/.env существует с вашим API-ключом
docker compose up --build
```

- Клиент: `http://localhost:80`
- Сервер: `http://localhost:8080`

---

###  Локально npm run

```bash
npm install # Установка зависимостей для клиента и сервера
# 1 вариант
npm run dev:server # Запуск сервера (Express.js)
npm run dev:client # Запуск клиента (Vite)
# 2 вариант (параллельный запуск)
npm run dev         # внутри корня проекта (frontend)
cd server && npm run dev # внутри папки server (backend)
```

## Запуск тестов

```bash
npm test          # Однократный запуск
npm run test:watch # Режим наблюдения
```

---

## Структура проекта

```
├── src/
│   ├── app/
│   │   ├── components/
│   │   │   ├── edit/          # Компоненты редактирования (поля по категориям, чат, diff)
│   │   │   ├── Header.tsx
│   │   │   └── Layout.tsx
│   │   ├── contexts/
│   │   │   └── ThemeContext.tsx # Контекст тёмной/светлой темы
│   │   ├── pages/
│   │   │   ├── CardsPage.tsx   # Список объявлений
│   │   │   ├── CardDetailPage.tsx # Просмотр объявления
│   │   │   └── CardEditPage.tsx   # Редактирование объявления
│   │   ├── services/
│   │   │   ├── api.ts          # API для работы с объявлениями
│   │   │   └── aiApi.ts        # API для AI-функций
│   │   ├── store/              # Redux Toolkit slices
│   │   ├── types/              # TypeScript типы
│   │   └── utils/              # Утилиты (категории, needsRevision)
│   └── styles/
├── server/
│   └── src/
│       ├── routes/
│       │   ├── items.ts        # CRUD для объявлений
│       │   └── ai.ts           # Прокси к OpenRouter API
│       ├── prompts/
│       │   └── safety.md       # Правила безопасности для LLM
│       ├── data.ts             # Начальные данные (25 объявлений)
│       ├── types.ts
│       └── utils.ts
├── docker-compose.yml
├── Dockerfile
└── nginx.conf
```

---

## Реализованный функционал

### Обязательные требования

1. **Страница списка объявлений** (`/ads`)
   - Сортировка: по названию (А→Я, Я→А), новизне, цене
   - Мультикатегорийный фильтр (чекбоксы)
   - Фильтр «Только требующие доработок»
   - Серверная пагинация (10 на страницу)
   - Переключатель сетка/список
   - Кнопка сброса фильтров
   - Пагинация с сохранением текущих фильтров и сортировки при навигации между страницами
   - Количество объявлений отображается с учётом текущих фильтров

2. **Страница просмотра** (`/ads/:id`)
   - Полная информация: название, цена, категория, дата публикации/редактирования
   - Характеристики зависят от категории (показываются только заполненные)
   - Баннер «Требуются доработки» с перечнем пустых полей (вычисляется клиентски)
   - Описание: текст или «Отсутствует»
   - Отображается дата создания и редактирования
   - Реализован библиотечный слайдер для фотографий (потому что в ТЗ были несостывки, то говорилось, что надо сделать, то не надо, так что было добавлено для полноты интерфейса)

3. **Страница редактирования** (`/ads/:id/edit`)
   - Категория влияет на набор характеристик:
     - Электроника: тип, бренд, модель, состояние, цвет
     - Транспорт: бренд, модель, год, КПП, пробег, мощность
     - Недвижимость: тип, адрес, площадь, этаж
   - AI-функции: «Придумать/Улучшить описание» с визуальным сравнением «Было → Стало» (word-level diff), «Узнать рыночную цену»
   - AI-чатбот с контекстом объявления (вопрос-ответ, сохранение истории в localStorage) + безопасность от prompt injection.
   - Сохранение черновика в localStorage с предложением восстановить
   - Валидация обязательных полей
   - Попапы с действиями для формы (success / error /warning)

4. **Серверная часть** — Express.js API с эндпоинтами GET /items, GET /items/:id, PUT /items/:id

5. **LLM-интеграция** — OpenRouter API (серверный прокси, ключ на бэкенде /server/.env)

### Дополнительные требования (со звёздочкой)

1. **Docker Compose** — клиент (nginx) + сервер запускаются одной командой
2. **Безопасность LLM** — правила в `server/src/prompts/safety.md`, защита от prompt injection
3. **Прерывание запросов** — AbortController при навигации между страницами (в Redux thunks и AI-запросах)
4. **Юнит-тесты** — Vitest + Testing Library (24 теста: утилиты, Redux slices)
5. **Чат с AI** — панель чата на странице редактирования с контекстом объявления
6. **Визуальное сравнение «Было → Стало»** — word-level diff при генерации описания
7. **Тёмная тема** — переключатель в шапке, сохранение в localStorage, Ant Design darkAlgorithm

---

## Принятые решения

1. **Переписан бек-енд самостоятеьно, для гибкости и возможности кастомизировать данные**.

---

## Возможные улучшения (формат заметок автора)

1. github actions + pages
2. sast анализ
3. интеграция тестов в CI/CD
4. env в настройки репозитория
5. добавить в Packages контейнеры
6. добавить ссылку на онлайн демо

